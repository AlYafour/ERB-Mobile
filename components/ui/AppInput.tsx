/**
 * Canonical name for the design-system text input.
 * The implementation lives in Input.tsx (tokens, focus ring, error/hint
 * states) — this alias keeps every design-system component under the App*
 * namespace without duplicating code.
 */
export { Input as AppInput } from '@/components/ui/Input';
